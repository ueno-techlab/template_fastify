# Docker User Configuration (for file permissions)
# Automatically set to your host user's UID/GID to avoid permission issues
# Run: id -u to get your UID, id -g to get your GID
USER_UID=1000
USER_GID=1000

# Server
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Database
DATABASE_URL=postgresql://user:password@db:5432/app

# JWT
JWT_SECRET=change-me-to-a-secure-random-string-at-least-32-characters
JWT_EXPIRES_IN=24h

# Logging
LOG_LEVEL=debug
LOG_DIR=./logs
LOG_MAX_SIZE=10M
LOG_MAX_AGE_DAYS=30
LOG_PRETTY=true

# Prisma Logging (development: query,error,warn | production: error)
PRISMA_LOG_LEVEL=query,error,warn
